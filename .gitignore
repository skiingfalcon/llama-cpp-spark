# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
.pytest_cache/
.ruff_cache/
*.egg

# llama.cpp vendor build (cloned + compiled locally)
vendor/

# Local model copies (shared store is /opt/models)
models/
*.gguf

# Eval corpora and cloned external harnesses (regenerate with local-llm eval …)
evals/data/
evals/tools/

# Runtime state (PIDs, llama-server logs); eval artifacts are committed
state/*
!state/evals/
.state/
*.pid
.env

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
