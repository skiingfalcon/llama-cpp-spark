"""AMD Strix Halo (Ryzen AI Max, Radeon 8060S / gfx1151) running Windows.

Backends are llama.cpp's Vulkan and HIP (ROCm) builds, installed as prebuilt release zips;
both can be present and are selected per serve with ``--backend``.
"""
